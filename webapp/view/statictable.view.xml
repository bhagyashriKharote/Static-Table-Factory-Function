<mvc:View
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    controllerName="statictable.controller.statictable"
>
    <Table
        id="IDGenTable"
        columns="{ path: 'columns>/TableColumns', factory: '.createColumn' }"
        items="{ path: '/ProductCollection', templateShareable: true, factory: '.createRow' }"
    />
</mvc:View>
